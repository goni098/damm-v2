[package]
name = "derive-variant-count"
version = "0.1.0"
description = "Derive macro that exposes a VARIANT_COUNT constant for structs (field count) and enums (variant count)."
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = "2.0"
quote = "1.0"
